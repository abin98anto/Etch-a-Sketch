STEPS:
1.Create all 3 files.						:done
2.Create heading div and the main div.				:
3.complete the main div with grids.				:
4.Create the hover effect.					:
5.Create the pop up for chossing the number of grids per side.	:
6.Have the hover effect have random RGB colour.			:
